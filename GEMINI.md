# Active State Hub

| Task | Internal Thought (z) | External Action (a) | Validator (v) | Status | Commit |
| --- | --- | --- | --- | --- | --- |
| **1: Environment Setup & DB Schema** | Need to set up `uv`, `psycopg2-binary` (ARM compatible), and PostgreSQL via Docker. Create tables: `customers`, `clusters_summary`, `campaign_copy`. | `uv init && uv add fastapi uvicorn psycopg2-binary sqlalchemy scikit-learn pandas google-adk pytest pydantic-settings python-dotenv` + Create `docker-compose.yml`, `app/db/schema.sql`, `tests/test_db.py`. | `docker-compose up -d && pytest tests/test_db.py` | Success | `chore: setup uv, docker and db schema` |
| **2: Mock Data Generator** | Need a script to populate `customers` table. Originally synthetic RFM data, now using actual CSV `g2_campana_email.csv`. | Create `scripts/seed_data.py` reading CSV via pandas and bulk inserting to DB. Create `tests/test_seed.py`. | `uv run python scripts/seed_data.py && uv run pytest tests/test_seed.py` | Success | `test: add data generator using real csv` |
| **3: DBSCAN Clustering Service** | Implement unsupervised learning. Scale data using `StandardScaler` then `DBSCAN`. Calculate centroids. Update DB. Must handle `eps` finding or use static values. | Create `app/services/clustering.py`. Create unit tests mocking DB. | `pytest tests/test_clustering.py` | Success | `feat: implement dbscan clustering service` |
| **4: ADK & OpenRouter Agent Config** | Configure `LlmAgent` from `google-adk` with `LiteLlm` targeting `openrouter/anthropic/claude-3.5-sonnet`. Secure API keys. | Create `app/agents/copywriter.py` and `tests/test_copywriter.py`. Handle `.env` loading safely. | `pytest tests/test_copywriter.py` | Success | `feat: configure adk llm agent with openrouter` |
| **5: Agent Orchestration** | Bridge clustering and copywriting. Iterate over `clusters_summary`, call the agent for each, save to `campaign_copy`. | Create `app/services/orchestrator.py` and `tests/test_orchestrator.py`. | `pytest tests/test_orchestrator.py` | Success | `feat: implement adk orchestrator for copy generation` |
| **6: FastAPI Endpoints & E2E** | Expose asynchronous `POST /api/v1/campaign/run` to trigger the pipeline and `GET` for results. Tie it all together. | Create `app/main.py`, `app/api/endpoints.py`, and `tests/test_api.py`. | `pytest tests/test_api.py` | Pending | `feat: add fastapi endpoints and e2e integration` |
