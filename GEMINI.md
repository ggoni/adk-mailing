# Active State Hub

| Task | Internal Thought (z) | External Action (a) | Validator (v) | Status | Commit |
| --- | --- | --- | --- | --- | --- |
| **1: Environment Setup & DB Schema** | Need to set up `uv`, `psycopg2-binary` (ARM compatible), and PostgreSQL via Docker. Create tables: `customers`, `clusters_summary`, `campaign_copy`. | `uv init && uv add fastapi uvicorn psycopg2-binary sqlalchemy scikit-learn pandas adk-python pytest pydantic-settings python-dotenv` + Create `docker-compose.yml`, `app/db/schema.sql`, `tests/test_db.py`. | `docker-compose up -d && pytest tests/test_db.py` | Pending | `chore: setup uv, docker and db schema` |
| **2: Mock Data Generator** | Need a script to populate `customers` table with synthetic RFM data to test DBSCAN without real data yet. | Create `scripts/seed_data.py` generating 500 rows using pandas/numpy and bulk inserting to DB. Create `tests/test_seed.py`. | `python scripts/seed_data.py && pytest tests/test_seed.py` | Pending | `test: add mock data generator for customers` |
| **3: DBSCAN Clustering Service** | Implement unsupervised learning. Scale data using `StandardScaler` then `DBSCAN`. Calculate centroids. Update DB. Must handle `eps` finding or use static values. | Create `app/services/clustering.py`. Create unit tests mocking DB. | `pytest tests/test_clustering.py` | Pending | `feat: implement dbscan clustering service` |
| **4: ADK & OpenRouter Agent Config** | Configure `LlmAgent` from `adk-python` with `LiteLlm` targeting `openrouter/anthropic/claude-3.5-sonnet`. Secure API keys. | Create `app/agents/copywriter.py` and `tests/test_copywriter.py`. Handle `.env` loading safely. | `pytest tests/test_copywriter.py` | Pending | `feat: configure adk llm agent with openrouter` |
| **5: Agent Orchestration** | Bridge clustering and copywriting. Iterate over `clusters_summary`, call the agent for each, save to `campaign_copy`. | Create `app/services/orchestrator.py` and `tests/test_orchestrator.py`. | `pytest tests/test_orchestrator.py` | Pending | `feat: implement adk orchestrator for copy generation` |
| **6: FastAPI Endpoints & E2E** | Expose asynchronous `POST /api/v1/campaign/run` to trigger the pipeline and `GET` for results. Tie it all together. | Create `app/main.py`, `app/api/endpoints.py`, and `tests/test_api.py`. | `pytest tests/test_api.py` | Pending | `feat: add fastapi endpoints and e2e integration` |
